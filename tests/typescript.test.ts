import { suite } from '..';
import * as assert from 'assert';

const test = suite('Typescript test');
test('runs', async () => {
  assert.ok(true);
});
// test('fails', async () => {
//   assert.ok(false);
// });
